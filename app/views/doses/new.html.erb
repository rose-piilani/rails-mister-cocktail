<div class="container">
  <div class="row">
    <%= simple_form_for ([@cocktail, @dose]) do |f| %>
      <%= f.input :description %>
      <%= f.input :ingredient_id, collection: Dose::INGREDIENTS %>
      <%= f.button :submit %>
    <% end %>
</div>
</div>
